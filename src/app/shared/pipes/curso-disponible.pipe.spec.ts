import { CursoDisponiblePipe } from './curso-disponible.pipe';

describe('CursoDisponiblePipe', () => {
  it('create an instance', () => {
    const pipe = new CursoDisponiblePipe();
    expect(pipe).toBeTruthy();
  });
});
